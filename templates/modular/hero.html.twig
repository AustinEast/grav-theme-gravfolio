<header>
        <div>
            <img src="{{ page.media[header.headshot].cropZoom(150,150).url }}">
            <h1>
                {{ site.author.name }}
            </h1>
                {% if page.content %}
                {{ page.content }}
                {% else %}
                {{ site.metadata.description }}
                {% endif %}
            {% include 'partials/social.html.twig' %}
        </div>
</header>